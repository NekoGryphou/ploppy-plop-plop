name: Release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: read

jobs:
  plugin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with: { node-version: 22, cache: npm, cache-dependency-path: decky/package-lock.json }
      - run: npm --prefix decky ci
      - run: ./scripts/decky/build-decky-deps.sh
      - run: npm --prefix decky test
      - run: npm --prefix decky run build
      - run: ./scripts/decky/build-plugin.sh
      - run: npm --prefix decky sbom --package-lock-only --sbom-format cyclonedx > out/plugin/DekyMyRig_Plugin.cdx.json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: DekyMyRig_Plugin
          path: |
            out/plugin/DekyMyRig_Plugin.zip
            out/plugin/DekyMyRig_Plugin.cdx.json

  windows:
    runs-on: windows-latest
    environment: production
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
        with:
          toolchain: 1.98.0
          components: rustfmt, clippy
          targets: x86_64-pc-windows-msvc
      - uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with: { dotnet-version: 8.0.x }
      - uses: crazy-max/ghaction-chocolatey@dfdcf5bba9c0a16358a21c13a06ec5c89024b3ac # v4.1.0
        with: { args: install innosetup --yes --no-progress }
      - name: Import release signing certificate
        if: ${{ !contains(github.ref_name, '-alpha.') }}
        shell: pwsh
        env:
          CERTIFICATE_BASE64: ${{ secrets.WINDOWS_CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:CERTIFICATE_BASE64)) { throw "WINDOWS_CERTIFICATE_BASE64 is required." }
          $path = Join-Path $env:RUNNER_TEMP "release-signing.pfx"
          [IO.File]::WriteAllBytes($path, [Convert]::FromBase64String($env:CERTIFICATE_BASE64))
          $password = ConvertTo-SecureString $env:CERTIFICATE_PASSWORD -AsPlainText -Force
          $certificate = Import-PfxCertificate -FilePath $path -CertStoreLocation Cert:\CurrentUser\My -Password $password
          "SIGNING_CERTIFICATE_THUMBPRINT=$($certificate.Thumbprint)" >> $env:GITHUB_ENV
          "REQUIRE_CODE_SIGNING=1" >> $env:GITHUB_ENV
          Remove-Item $path -Force
      - shell: pwsh
        run: ./scripts/windows/build-windows.ps1
      - shell: pwsh
        run: ./scripts/windows/test-windows.ps1
      - run: cargo install cargo-cyclonedx --locked
      - run: cargo cyclonedx --manifest-path host/Cargo.toml --format json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: DeckyMyRig_Host__Windows_x64
          path: |
            out/host/DeckyMyRig_Host__Windows_x64.exe
            host/decky-my-rig-host.cdx.json

  publish:
    needs: [plugin, windows]
    runs-on: windows-latest
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: "*MyRig_*"
          path: release
          merge-multiple: true
      - name: Verify tag and build release manifest
        shell: pwsh
        run: |
          $version = (Get-Content decky/package.json | ConvertFrom-Json).version
          $stableTag = "v$version"
          $alphaPattern = '^' + [Regex]::Escape($stableTag) + '-alpha\.[1-9]\d*$'
          if (($env:GITHUB_REF_NAME -ne $stableTag) -and ($env:GITHUB_REF_NAME -notmatch $alphaPattern)) {
            throw "Tag $env:GITHUB_REF_NAME does not match package version $version."
          }
          ./scripts/release/create-manifest.ps1 -Version $version -ReleaseTag $env:GITHUB_REF_NAME -ArtifactDirectory release -Repository $env:GITHUB_REPOSITORY
      - uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: |
            release/DeckyMyRig_Host__Windows_x64.exe
            release/DekyMyRig_Plugin.zip
            release/release-manifest.json
      - name: Publish immutable release assets
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: |
          if ($env:GITHUB_REF_NAME -match '-alpha\.[1-9]\d*$') {
            gh release create $env:GITHUB_REF_NAME release/* --verify-tag --prerelease --title "Decky My Rig $env:GITHUB_REF_NAME" --notes "Alpha preview. The Windows host is currently unsigned and Microsoft Defender SmartScreen will warn during installation. Verify SHA256SUMS.txt before running it. Automatic host updates intentionally reject this unsigned build."
          } else {
            gh release create $env:GITHUB_REF_NAME release/* --verify-tag --generate-notes --title "Decky My Rig $env:GITHUB_REF_NAME"
          }
